# Plan — wave follow-up B: compass carriers, bugs as current truth, the id registry, visibility and placement (schema 3.4, fifth revision in place)

Date: 2026-09-17
Source: the parallel-wave improvement brief, `.cortex/archive/documents/parallel-wave-brief-2026-09-14/extracted/{asks,claims}.md` (asks A-06, A-08, A-11, A-12, A-14, A-15). Pedro approved the whole set, contract changes included, on 2026-09-17.
Specs (new, draft): `.specflow/specs/schema/id-registry.spec.md` (`schema.id-registry`),
`.specflow/specs/schema/visibility.spec.md` (`schema.visibility`),
`.specflow/specs/compass/bug-currency.spec.md` (`compass.bug-currency`).
Specs (amended in place): `recall.recall-index` Rules 1, 8, 13, new 15–17 + four ACs;
`hooks.search-annotate` Rules 7, 8 + two ACs; `hooks.pre-read-writeback` Rule 6 (`Bugs:` part) + one AC;
`recall.why` Rules 1, 4, 5 + two ACs; `pulse.usage` Rule 11 (`Bugs:` prefix, `R-`/`B-` id tokens);
`pulse.threads` Rule 12 (registry allocation, `found_at_commit`); `pulse.review-cli` Rules 7, 7b;
`core-cli.init` Rules 3, 10 + one AC; `core-cli.sync` Rules 3, 10, 15 + one AC (+ one amended);
`archive.ingest-skill` Entities + one AC (B-020); `insight.promotion-mechanism` Entities, Rules 5, 6, 7, ACs (S-055 split into insight-/archive-sourced, S-056 widened, S-061 added), Notes (B-020 — Pedro's reading (a): schema §4.5.1 wins).
Business outcomes amended: `schema.contributor-trusts-project-knowledge` (implemented_by + Notes),
`scaffolding.assistant-reaches-for-cortex-instead-of-guessing` (implemented_by + Notes),
`core-cli.developer-sets-up-cortex-in-one-command` (Notes), `core-cli.developer-keeps-existing-project-current` (rule 3 + Notes).
Contract: `cortex-schema.md` **3.4, fifth revision in place** — header note; §4.1 (id allocation); §4.2 (three optional fields, `triaged`, allocation, `check.bug`); §4.11 (inputs, subjects, entailment 4, entries, check, consumers, example); §8 (protocol roles, placement paragraph, mandate removed, revision paragraph); §10.1 (`visibility`, `placement`); §10.4 (fifth-revision migration); §4.5 (`promotion` Source clause corrected per B-020); Appendix A (+`check.id-registry`, +`check.visibility`). RULES.md rule 20 (+ rule 12 cross-reference).
Skills (already edited and mirrored byte-identically into `.claude/skills/`): `cortex-archive-ingest` (report path), `specflow-bugs` (registry allocation, `found_at_commit`), `specflow-entry` (coordinator-style requests read compass first), `specflow-spec-editor` (rule ids through the registry).
**Already written** — no schema, RULES, spec or skill edits in this plan.
**Schema follow-ups landed (after wave A committed at `1a0174c`):** §5 grammar (`Bugs:` prefix, `Recall:` kinds), §1 layout (`compass/registry.md`), Appendix A in-place rows for `check.bug`, `check.recall-index`, `check.config`, and the check-count paragraph. Nothing is left for the schema owner.
Status: **ready to execute** (no code or tests exist yet).

## Ground rules for every task

- **Tests first.** `specflow-tests` writes the test file named in the task before the
  implementation file; the task is done when the named `pnpm vitest run …` command is green.
- **R-001 governs** `src/**` (regex predicate: no LLM SDK import). Nothing here imports an SDK,
  spawns a subprocess — **in particular no `git`** (`readHeadCommit` reads `.git/HEAD` by file
  I/O; the registry's merge conflict is git's, not ours) — or touches the network.
- **RULES.md rule 6.** Hooks keep exit 0 always; the PreRead `Bugs:` part rides the `allow`
  envelope only; nothing new denies.
- **RULES.md rule 7.** Loops never write `compass/registry.md`: `session-observe` reads it;
  `pulse-accept`, `thread promote`, `cortex id next`, `sync` and `init` are the only writers.
- **RULES.md rule 11 budgets** are chars/4: the search pointer ≤240 chars over ≤2 lines
  (unchanged); the PreRead marker ≤200 chars, combined ceiling 400/500 (unchanged); the CLAUDE.md
  block must not grow — the mandate removal (~190 chars) pays for the role sentence and the
  placement paragraph; assert `claudeMdBlock(...)` length ≤ the pre-change length in the init test.
- **RULES.md rule 19:** each batch report enumerates every contract surface touched. The full set
  for this wave: `RECALL_ENTRY_KINDS` (+3); `RecallSubject` (+`rules`, +`bugs`); `scanCarriers`
  (+3 sources); `keywordsOf`/a new `docKeywords`; `compileRecallIndex` (entailment 4, derived
  subjects); `checkRecallIndex` (optional lists, kinds); `recallLine` (empty date, id-before-title
  for `rule`/`bug`); `selectPointers` strength order; `markerLine` (+`Bugs:`; source-file
  bugs-only path); `isMarkedTarget` callers in `pre-read.ts`; `pointerTargetsIn` prefix regex
  (+`Bugs:`) and `ID_SHAPES` (+`R-`/`B-`); `cortex why` listing (+`Bugs:`/`Rules:` sections,
  bug frontmatter read, `--json` `bugs`); `cortex recall --kind` enum; `checkBugs` (+3 fields);
  `readHeadCommit` (new); `thread promote --to compass/bugs` (registry id, stamp);
  `REGISTRY_HEADER`/`allocateId`/`parseRegistry` (new); `checkIdRegistry` (new); `cortex id next`
  (new verb; `USAGE_TABLE` +1 row and the docblock mirror); `pulse-accept` rule-file registration
  and refusal; `nextRuleId` floor; `sync` Rule 15; `init` skeleton (registry, `visibility` in
  `CONFIG_DEFAULTS`); `checkConfig` (`visibility`, `placement`; `knownKeys` +2);
  `checkVisibility`/`VISIBILITY_PATTERNS`/`VISIBILITY_CONTEXT_WORDS`/`VISIBILITY_SAFE_HOSTS` (new);
  `validate.ts` (+2 checks); `claudeMdBlock(projectName, config)` (signature, role sentence,
  placement paragraph, mandate removed) and its two callers.
- **Grammar is pinned.** The role sentence, the placement paragraph's three tails, the `Recall:`
  no-date form, the `Bugs:` marker part, the `why` `Bugs:`/`Rules:` sections and the registry
  header are quoted in the specs. Do not "improve" the wording.
- **Standing authorities:** the engineering-call constants are already in the specs
  (`RULE_GOVERNS_SUBJECT_CAP = 20`, compass-doc keyword cap 40, `Bugs:` max 2, per-file
  visibility cap 20, 7-character sha, registry line grammar); do not re-decide them.
- **Parallel-batch caveat:** a batch's own suite run can see a sibling's in-flight edits; only
  the orchestrator's full `pnpm test` after all batches is authoritative
  (`compass/standing-authorities.md`).
- **Concurrent wave A.** Another pass is editing `src/schema/checks/xref.ts`,
  `src/schema/index-build.ts`, `src/schema/checks/compass.ts` (H1 agreement), `src/insight/cli.ts`,
  `src/hooks/pre-read.ts` (Rules 2 and 7), `src/hooks/session-end.ts` and
  `tests/atomic/schema/validator.test.ts`. Batches 1 and 2 below touch `pre-read.ts` (Rule 6 only)
  and `compass.ts` (`checkBugs` only); add code in new functions and new test files, never
  reflow existing lines, and merge by hand if the file moved.
- **Insight is current** for `src/schema/checks/compass.ts`, `src/schema/checks/config.ts`,
  `src/cli/scaffold.ts`, `src/cli/templates.ts`, `src/hooks/pre-read.ts`, `src/pulse/usage.ts`,
  `src/cli/sync.ts`, `src/cli/init.ts`, `src/insight/session-observe.ts`, `src/pulse/review.ts`
  (`cortex insight file <path>`); there is **no entry** for `src/recall/index.ts`,
  `src/recall/query.ts`, `src/recall/cli.ts`, `src/pulse/thread-cli.ts` — read them.

## Exploration summary

- **Relevant existing code:** `src/recall/index.ts` — `RECALL_ENTRY_KINDS` (52),
  `RecallSubject` (55), `RecallEntry` (66), `Carrier` (86, `kind`/`id`/`title`/`path`/`date`/
  `bearsOn`/status fields), `scanAtlasKind` (178), `scanCarriers` (265), `keywordsOf` (280),
  `entryOf` (286), `compileRecallIndex` (339), `writeRecallIndex` (431), private
  `THREAD_TITLE_MAX`/`KEYWORD_MIN_CHARS`; `src/recall/query.ts` — `loadRecallIndex` (82),
  `tokenise` (139, refs regex already yields `R-NNN`/`B-NNN`), `candidateKeys` (253),
  `keywordMatches` (295), `recallLine` (335), `decidedLine` (340), `moreTail` (345),
  `markerLine` (373), `selectPointers` (439), `cutTitle` (324), `POINTER_BUDGET_CHARS` (319);
  `src/hooks/pre-read.ts` — `isMarkedTarget` (198), `mergeSubjects` (203), `fitMarker` (219),
  `recallMarker` (241), `MARKER_MAX_CHARS` (75), the Rule 6 branch at `run` line ~390;
  `src/recall/cli.ts` — the `why` listing and `--json`, the `recall --kind` enum;
  `src/pulse/usage.ts` — `pointerTargetsIn` (268, regex `/^(Recall|Decided|Evidence|Open):/`),
  `ID_SHAPES`, `MORE_TAIL_RE` (250); `src/schema/checks/compass.ts` — `checkBugs` (112–167,
  per-field blocks to copy for the three new fields); `src/schema/checks/recall-index.ts`;
  `src/pulse/thread-cli.ts` — `nextBugId` (254–260, replace), the promote frontmatter builder
  (~289); `src/insight/session-observe.ts` — `nextRuleId` (659–675); `src/pulse/review.ts` —
  the accept path's target-root check and file append; `src/pulse/suggestion-ids.ts` — the
  counter idiom (read, bump, write) to mirror for the registry's append; `src/cli/cli.ts` —
  `USAGE_TABLE` (61–92) and the docblock `Verbs:` line (49) mirrored by
  `tests/atomic/core-cli/dispatch.test.ts`; `src/cli/templates.ts` — `CONFIG_DEFAULTS` (26),
  `claudeMdBlock` (347–400), `CORTEX_INDEXES['compass']` (74); `src/cli/scaffold.ts` —
  `upsertClaudeMd` (370–403, calls `claudeMdBlock(projectName)` at 384); `src/cli/init.ts:561`
  and `src/cli/sync.ts:536` — the two `upsertClaudeMd(absRoot)` callers; `src/cli/sync.ts` —
  the Rule 4→5 seam and the summary object; `src/schema/checks/config.ts` — `knownKeys` (112),
  the `profile` block as the typed-key template; `src/schema/validate.ts` — the check call list.
- **Established patterns:** checks are `checkX(root, index?) → Violation[]` with
  `{ severity, check, clause, location: { path, key|line }, message }`; hooks are
  `run(stdinJson, opts?) → HookRunResult`, expected-empty → `SILENT`; per-project counters read
  a plain-text file, bump, write; tests build temp roots with `fs.mkdtempSync` and write fixtures
  directly; spec-layer tests drive the CLI entry points; `gray-matter` parses frontmatter;
  `picomatch` and `fast-glob` are already dependencies for glob expansion; `ignore` is already a
  dependency (used by the scanner) for gitignore evaluation.
- **Tech stack:** Node ≥20, TypeScript strict, vitest, no new dependency (RULES.md rule 2 not
  triggered).

## Gap analysis

- **Can reuse:** the `Carrier` shape (add three kinds); `keywordsOf`; `tokenise`'s ref regex;
  `candidateKeys` (already maps `R-NNN`/`B-NNN` paths to ids); `recallLine`/`cutTitle`/`moreTail`;
  `fitMarker`'s cut ladder; the `profile` typed-key block in `config.ts`; `globMatchesNothing`'s
  glob helper for `governs` expansion; the suggestion-counter read/bump/write idiom; `checkBugs`'s
  per-field violation blocks; `gray-matter`.
- **Must create:** `src/compass/registry.ts`, `src/compass/git-head.ts`,
  `src/schema/checks/registry.ts`, `src/schema/checks/visibility.ts`; tests
  `tests/atomic/recall/compass-carriers.test.ts`, `tests/atomic/compass/registry.test.ts`,
  `tests/atomic/compass/git-head.test.ts`, `tests/atomic/schema/id-registry-check.test.ts`,
  `tests/atomic/schema/visibility-check.test.ts`, `tests/atomic/schema/bug-currency-check.test.ts`,
  `tests/spec/schema/id-registry.spec.test.ts`, `tests/spec/schema/visibility.spec.test.ts`,
  `tests/spec/compass/bug-currency.spec.test.ts`, `tests/atomic/core-cli/id-next.test.ts`,
  `tests/atomic/core-cli/claude-md-block.test.ts`.
- **Must modify:** `src/recall/index.ts`, `src/recall/query.ts`, `src/recall/cli.ts`,
  `src/hooks/pre-read.ts` (Rule 6 branch only), `src/pulse/usage.ts`,
  `src/schema/checks/recall-index.ts`, `src/schema/checks/compass.ts` (`checkBugs` only),
  `src/schema/checks/config.ts`, `src/schema/validate.ts`, `src/pulse/thread-cli.ts`,
  `src/pulse/review.ts`, `src/insight/session-observe.ts`, `src/cli/cli.ts`,
  `src/cli/templates.ts`, `src/cli/scaffold.ts`, `src/cli/init.ts`, `src/cli/sync.ts`; tests
  `tests/atomic/recall/index.test.ts`, `tests/spec/recall/recall-index.spec.test.ts`,
  `tests/atomic/recall/query.test.ts`, `tests/atomic/hooks/search-annotate.test.ts`,
  `tests/spec/hooks/search-annotate.test.ts`, `tests/atomic/hooks/pre-read.test.ts`,
  `tests/spec/hooks/pre-read.test.ts`, `tests/atomic/recall/cli.test.ts`,
  `tests/spec/recall/why.spec.test.ts`, `tests/atomic/pulse/usage.test.ts`,
  `tests/atomic/schema/recall-index-check.test.ts`, `tests/atomic/pulse/thread-cli.test.ts`,
  `tests/spec/pulse/thread-cli.spec.test.ts`, `tests/atomic/pulse/review-cli.test.ts`,
  `tests/atomic/insight/session-observe.test.ts`, `tests/atomic/core-cli/dispatch.test.ts`,
  `tests/atomic/core-cli/init.test.ts` (line 224 config shape), `tests/spec/core-cli/init.test.ts`,
  `tests/atomic/core-cli/sync.test.ts`, `tests/spec/core-cli/sync.test.ts`,
  `tests/atomic/schema/config-check.test.ts`.
- **Open questions (assumptions stated, proceed):** (1) `governs` expansion uses `fast-glob`
  with `{ cwd: root, onlyFiles: true, dot: false, ignore: ['node_modules/**', '.git/**'] }`
  and sorts results; the literal prefix is the glob up to the first of `* ? [ {`, trailing `/`
  stripped. (2) Bold spans are `**…**` and `__…__` runs; headings are lines starting `#`;
  tokenisation reuses `tokenise`'s rules (≥3 chars, `STOP_TOKENS` dropped). (3) The bug `date`
  is `opened` as written (an iso string) or `""`. (4) `readHeadCommit` follows exactly one
  `gitdir:` and one `commondir` indirection. (5) The registry is written with a trailing newline
  and `\n` line endings; a CRLF file is parsed tolerantly and rewritten with `\n`. (6) The
  `ignore` evaluation reads the root `.gitignore` and `.cortex/.gitignore` only; nested ignore
  files are out of scope. (7) `claudeMdBlock`'s second argument is the parsed config object
  (`Record<string, unknown>`), read by the caller from `cortex.config.json`; a missing or
  unparseable config renders `unknown` and no notes directory.

## Size check and batching

Three new specs plus ten amended, roughly 4,000 lines of relevant existing code — well over the
one-agent heuristic, so it is **split into four batches with disjoint file ownership**. Batches
1–4 are independent of each other except that batch 2's `thread promote` task needs batch 3's
`allocateId`; run batch 3 first (or run 1, 3 and 4 in parallel and 2 after 3), each at Light
depth.

```
Batch 3  id registry ─────────────────────────────────────┐
  (owns src/compass/registry.ts, src/schema/checks/registry.ts,│
   src/pulse/review.ts, src/insight/session-observe.ts,       │
   src/cli/cli.ts [id verb + USAGE_TABLE], src/cli/sync.ts     ├─► Batch 2  bug fields + git-head + promote
   [Rule 15], src/cli/init.ts [registry skeleton],            │   (owns src/compass/git-head.ts, src/schema/checks/compass.ts
   src/cli/templates.ts [CORTEX_INDEXES compass line only])   │    [checkBugs only], src/pulse/thread-cli.ts,
                                                              │     src/pulse/promote.ts, src/pulse/review.ts [promotion branch])
                                                              │
Batch 1  recall carriers + consumers (parallel with 3 and 4)  │
  (owns src/recall/**, src/schema/checks/recall-index.ts,     │
   src/hooks/pre-read.ts [Rule 6 branch], src/pulse/usage.ts) │
                                                              │
Batch 4  visibility + config + CLAUDE.md block (parallel)     │
  (owns src/schema/checks/visibility.ts, src/schema/checks/config.ts, src/schema/validate.ts [+2 checks],
   src/cli/templates.ts [CONFIG_DEFAULTS, claudeMdBlock], src/cli/scaffold.ts [upsertClaudeMd],
   the two upsertClaudeMd call sites in init.ts/sync.ts)
Orchestrator: pnpm test → pnpm build → cortex sync (this repo: regenerates the CLAUDE.md block, creates compass/registry.md)
              → cortex scan (rebuilds recall-index.json with the compass carriers) → cortex validate → specs to implemented → memory → commit
```

Ownership notes: `src/pulse/review.ts` is shared between batch 3 (Task 3.4, the accept path's rule-file registration, before the write) and batch 2 (Task 2.5, the promotion branch at ~637–651) — different regions of the accept function; batch 2 runs after batch 3 and rebases. `src/cli/templates.ts` is shared between batch 3 (one line in
`CORTEX_INDEXES['compass']`) and batch 4 (`CONFIG_DEFAULTS`, `claudeMdBlock`) — batch 3 makes its
edit first and batch 4 rebases; `src/cli/init.ts` and `src/cli/sync.ts` are shared the same way
(batch 3: skeleton/Rule 15; batch 4: the `upsertClaudeMd` call sites) — different functions,
coordinate at merge. `src/schema/validate.ts` registers `checkIdRegistry` (batch 3) and
`checkVisibility` (batch 4) — two adjacent lines; batch 4 adds both to avoid a conflict, and
batch 3's check file must exist first (create it as a stub in batch 3's first task).

---

## Batch 1 — the compass carriers and their consumers (~90 min)

### Task 1.1: Three new carrier kinds and two new subject lists in the index

**Criterion:** `recall.recall-index` — "A rule bears on its governs prefixes, its matched files and its related specs"; "A governs glob matching 30 files caps at 20 subjects, and a retired rule contributes nothing"; "A compass document is a keyword-only entry built from headings and bold spans"; "Only open and triaged bugs reach a subject"
**Files:** `tests/atomic/recall/compass-carriers.test.ts` (create), `src/recall/index.ts` (modify)
**Change:** Extend `RECALL_ENTRY_KINDS` to `['decision','evidence','thread','observation','rule','compass-doc','bug']`; add `rules: string[]` and `bugs: string[]` to `RecallSubject` (and to the empty-subject factory used by `compileRecallIndex`). Add three scanners called from `scanCarriers`: `scanRules(root)` — every `.cortex/compass/rules/R-*.md` whose frontmatter parses and whose `status !== 'retired'`, `id` = frontmatter `id`, `title`, `date: ''`, `bearsOn` = **derived** refs: for each `governs` glob, `globPrefix(glob)` (the literal part before the first `* ? [ {`, trailing `/` stripped) when `fs.statSync(root/prefix).isDirectory()`, plus `fast-glob.sync(glob, { cwd: root, onlyFiles: true, dot: false, ignore: ['node_modules/**','.git/**'] })` sorted, the first `RULE_GOVERNS_SUBJECT_CAP = 20` (exported) across all globs, plus each `related_specs` id; `scanBugs(root)` — every `compass/bugs/B-*.md`, `id`, `title`, `date` = `opened` string or `''`, `bearsOn` = `affects` (strings only), `status` kept on the carrier; `scanCompassDocs(root)` — the four fixed filenames (`COMPASS_DOC_FILES`, exported), `id: compass.<stem>`, `title` = first `# ` heading text or the stem, `date: ''`, `bearsOn: []`, plus a `docKeywords: string[]` field = `tokenise`-style tokens (≥3 chars, `STOP_TOKENS` removed) of every `#`-heading line and every `**…**`/`__…__` span in document order, first 40 distinct (`COMPASS_DOC_KEYWORD_CAP`). In `entryOf`, a `compass-doc` carrier's `keywords` = `docKeywords` (not title tokens); rule/bug keywords = `keywordsOf(title, bearsOn)` as today. In `compileRecallIndex`: rule refs resolve with `resolveRef` like any (path refs normalised), contributing `R.id` to `subjects[X].rules`; bug refs contribute `B.id` to `subjects[X].bugs` **iff** `status` is `open` or `triaged` (resolved bugs: entry only); unresolved refs bump `droppedRefs`. Keep every list sorted/deduped; keys sorted.
**Verify:** `pnpm vitest run tests/atomic/recall/compass-carriers.test.ts tests/atomic/recall/index.test.ts`

### Task 1.2: The check learns the lists and the kinds; the spec-layer index test covers them

**Criterion:** `recall.recall-index` — "The emitted file passes its own check"; "A malformed index is an error, an absent one is nothing"
**Files:** `tests/atomic/schema/recall-index-check.test.ts` (modify — three cases), `src/schema/checks/recall-index.ts` (modify), `tests/spec/recall/recall-index.spec.test.ts` (modify — one end-to-end case with a rule, a bug and `environment.md`)
**Change:** `checkRecallIndex`: accept the seven kinds; when a subject carries `rules`/`bugs`, require string arrays whose ids exist in `entries`; when absent, no violation. Spec test: write a fixture with `R-001` governing `src/db/**/*.ts`, `B-019` open affecting `src/db/schema.ts`, `environment.md` with a heading, run `writeRecallIndex`, assert `subjects['src/db'].rules`, `subjects['src/db/schema.ts'].bugs`, `entries['compass.environment'].kind`, and zero `check.recall-index` errors from `cortex validate`.
**Verify:** `pnpm vitest run tests/atomic/schema/recall-index-check.test.ts tests/spec/recall/recall-index.spec.test.ts`

### Task 1.3: `Recall:` lines for the new kinds and the new strength order

**Criterion:** `hooks.search-annotate` — "A keyword search lands on a compass document"; "A grep into a governed directory points at the rule, and an open bug outranks it"; "The imperative-free grammar"
**Files:** `tests/atomic/recall/query.test.ts` (modify), `tests/atomic/hooks/search-annotate.test.ts` (modify), `tests/spec/hooks/search-annotate.test.ts` (modify), `src/recall/query.ts` (modify)
**Change:** `recallLine(entry)`: when `entry.date === ''` omit the date and its space; for `kind` `rule` or `bug` insert the entry id before the title: `Recall: rule R-014 <title> (<path>)`, `Recall: bug 2026-06-28 B-031 <title> (<path>)`. `selectPointers`: strength order `threads > bugs > decided > rules > evidence > observations`; "newest first" sorts empty dates last then id ascending. `keywordMatches` already ranks by hits/date/id — confirm empty dates sort last. The `more:` tail and budget ladder unchanged.
**Verify:** `pnpm vitest run tests/atomic/recall/query.test.ts tests/atomic/hooks/search-annotate.test.ts tests/spec/hooks/search-annotate.test.ts`

### Task 1.4: The PreRead marker's `Bugs:` part, alone on a source file

**Criterion:** `hooks.pre-read-writeback` — "An open bug rides the marker last, and alone on a source file"; "A rule read with three of everything is cut and pointed onward" (unchanged, must stay green)
**Files:** `tests/atomic/hooks/pre-read.test.ts` (modify — add cases; do not reflow the Rule 7 cases wave A is editing), `tests/spec/hooks/pre-read.test.ts` (modify), `src/recall/query.ts` (modify — `markerLine`), `src/hooks/pre-read.ts` (modify — the Rule 6 branch at `run` ~390 and `recallMarker`/`fitMarker`)
**Change:** `markerLine(subject, key, index)` appends ` · Bugs: <ids, max 2>` (newest `opened` first from `entries[id].date`, empty last) after `Open:`; `fitMarker`'s ladder: drop `more:`, `Open:`→1, `Decided:`→1, `Bugs:`→1. In `run`: for a target that is **not** `isMarkedTarget` and not deferred (Rule 7 wins first — leave that ordering), look the normalised path up as a subject; if `subject.bugs.length > 0` build a marker from a subject copy with `decided`/`evidence`/`threads`/`observations` emptied (so the line is `Bugs: …` plus `more:` when cut) and append it after the summary/invitation/duplicate note within the existing combined budget; no subject or empty `bugs` → no marker, exactly as before. `MARKER_MAX_CHARS` unchanged.
**Verify:** `pnpm vitest run tests/atomic/hooks/pre-read.test.ts tests/spec/hooks/pre-read.test.ts`

### Task 1.5: `cortex why` lists bugs (with currency fields) and rules; `recall --kind` gains three kinds

**Criterion:** `recall.why` — "why lists the rule and the open bug on a source file, with the bug's currency fields"; "recall filters by a compass kind"; "--json is the subject block plus its entries"
**Files:** `tests/atomic/recall/cli.test.ts` (modify), `tests/spec/recall/why.spec.test.ts` (modify), `src/recall/cli.ts` (modify)
**Change:** Heading counts gain ` · <n> bugs · <n> rules`; after `Open:` render `Bugs:` (`<B-id>  <status> — <title>  (<path>)` newest first, sub-line `owner=<v>  fix=<v>  found_at=<v>` with `-` for absent, omitted when all three absent; `status` and the three fields read with `gray-matter` from the bug file at `entries[id].path`, unreadable → `(fields unreadable)`), then `Rules:` (`<R-id>  <title>  (<path>)` in id order, no read). `--json` subject carries six lists; add `"bugs": { <id>: { status, owner, fix_in_flight, found_at_commit } }` with `null` for absent. `--kind` enum = the seven kinds; the `recall` line format for an empty date drops the date field (`compass-doc  compass.environment — Environment (<path>)`).
**Verify:** `pnpm vitest run tests/atomic/recall/cli.test.ts tests/spec/recall/why.spec.test.ts`

### Task 1.6: `pulse.usage` counts the `Bugs:` prefix and the compass id shapes

**Criterion:** `pulse.usage` — Rule 11 (no new AC; the existing "pointer fired/followed" cases gain two rows)
**Files:** `tests/atomic/pulse/usage.test.ts` (modify — a `Bugs: B-019` marker followed by a Read of `.cortex/compass/bugs/B-019-x.md`; a `Recall: rule R-001 …` line followed by a Read of the rule file), `src/pulse/usage.ts` (modify)
**Change:** `pointerTargetsIn` prefix regex → `/^(Recall|Decided|Evidence|Open|Bugs):/`; `ID_SHAPES` gains `R-\d{3,}` → any path under `.cortex/compass/rules/` whose basename starts with the id, and `B-\d{3,}` → `.cortex/compass/bugs/` likewise. Nothing else in the follow logic changes.
**Verify:** `pnpm vitest run tests/atomic/pulse/usage.test.ts tests/spec/pulse/usage.test.ts`

---

## Batch 3 — the id registry, its verb, its check, its writers, and the sync migration (~80 min; run before batch 2)

### Task 3.1: The registry module — parse, allocate, create-from-disk

**Criterion:** `schema.id-registry` — "Allocation appends the next id in its kind's block"; "A file that predates the registry still raises the floor"; "An absent registry is created on first allocation"
**Files:** `tests/atomic/compass/registry.test.ts` (create), `src/compass/registry.ts` (create), `src/schema/checks/registry.ts` (create — an empty `checkIdRegistry` stub returning `[]`, filled in Task 3.3)
**Change:** Export `REGISTRY_FILE = '.cortex/compass/registry.md'`, `REGISTRY_HEADER` (the three header lines from Rule 1, verbatim), `RegistryLine = { id, slug, line }`, `parseRegistry(text): { lines: RegistryLine[]; malformed: {line, text}[]; duplicates: {id, lines:number[]}[] }` (blank and `#` lines ignored; grammar `/^(R|B)-(\d{3,}) ([a-z0-9-]+)$/`), `idsOnDisk(root, kind): number[]` (from filenames `R-NNN…`/`B-NNN…`), `buildRegistryFromDisk(root): string` (header + rules asc + bugs asc, slug = filename remainder after `<id>-` minus `.md`, or `reserved`), `allocateId(root, kind, slug?)`: read (or build-from-disk when absent), `next = max(registry, disk) + 1` zero-padded to 3, insert `<id> <slug|reserved>` after the last `R-` line for a rule (or after the header when none) and at end for a bug, write with `\n` and a trailing newline, return the id. Deterministic; never throws on a missing directory (creates `compass/`).
**Verify:** `pnpm vitest run tests/atomic/compass/registry.test.ts`

### Task 3.2: `cortex id next rule|bug [--slug <slug>]`

**Criterion:** `schema.id-registry` — "`cortex id next` prints the id and reserves it"; "Bad grammar is exit 2 and writes nothing"
**Files:** `tests/atomic/core-cli/id-next.test.ts` (create), `tests/atomic/core-cli/dispatch.test.ts` (modify — the docblock/usage mirror gains `id`), `src/cli/cli.ts` (modify)
**Change:** Add `['id', 'id next rule|bug [--slug <slug>]']` to `USAGE_TABLE` after `thread` and `id` to the docblock `Verbs:` line at the same position; dispatch `id next <kind> [--slug s]` → `allocateId`; print the id alone to stdout, exit 0; missing/unknown kind or flag → usage line on stderr, exit 2, nothing written; no `.cortex/cortex.config.json` → stderr names `cortex init`, exit 1. Count it under `pulse.usage` Rule 2's invocation regex only if that regex is a fixed verb list (it is `cortex (insight|recall|why)` — leave it; not a recall verb).
**Verify:** `pnpm vitest run tests/atomic/core-cli/id-next.test.ts tests/atomic/core-cli/dispatch.test.ts`

### Task 3.3: `check.id-registry`

**Criterion:** `schema.id-registry` — "A file missing from the registry is an error naming the remedy"; "A registered id without a file is a warning, not an error"; "Duplicate and malformed lines are errors"; "An absent registry warns only when there is something to register"
**Files:** `tests/atomic/schema/id-registry-check.test.ts` (create), `src/schema/checks/registry.ts` (modify — fill the stub), `src/schema/validate.ts` (modify — call `checkIdRegistry(root)` after `checkBugs`; **batch 4 also edits this call list — add both `checkIdRegistry` and `checkVisibility` here if batch 4 has not landed, else rebase**)
**Change:** `checkIdRegistry(root): Violation[]` with `check: 'check.id-registry'`, `clause: '§4.1'` for rule findings, `'§4.2'` for bug findings, `'§10.4'` for the absent-registry warning: absent + any rule/bug file → one warning naming `cortex sync`; malformed line → error with `location.line`; duplicate id → error naming both lines; file id on no line → error naming the file and `cortex id next <kind>`; line with no file → warning; out-of-order line → warning. Read-only.
**Verify:** `pnpm vitest run tests/atomic/schema/id-registry-check.test.ts`

### Task 3.4: `pulse-accept` registers a new rule file and refuses a taken id

**Criterion:** `schema.id-registry` — "Accept registers a new rule file, and refuses a taken id"; `pulse.review-cli` Rule 7b
**Files:** `tests/atomic/pulse/review-cli.test.ts` (modify — two cases), `src/pulse/review.ts` (modify)
**Change:** In the accept path, after the target-root check and before any write: if the target matches `/^\.cortex\/compass\/rules\/(R-\d{3,})-([a-z0-9-]+)\.md$/` and the file does not exist, `parseRegistry` (build-from-disk when absent, via `allocateId`'s helper without allocating); a line with that id and a different slug → print the line and `cortex id next rule`, exit 1, no writes; otherwise land the file and status as today, then append `<id> <slug>` (skip when a line with the same id and slug already exists). Existing targets are untouched.
**Verify:** `pnpm vitest run tests/atomic/pulse/review-cli.test.ts tests/spec/pulse/review-cli.test.ts`

### Task 3.5: `session-observe`'s drafted rule id reads the registry floor

**Criterion:** `insight.session-observe` — "A rule-candidate proposal is actually acceptable (B-010)" (unchanged; must stay green with a registry present)
**Files:** `tests/atomic/insight/session-observe.test.ts` (modify — one case: registry lists `R-009 reserved`, no `R-009` file, the next drafted target is `R-010`), `src/insight/session-observe.ts` (modify — `nextRuleId`)
**Change:** `nextRuleId` takes `max` over the directory scan **and** `parseRegistry` (when the file exists; read-only, never written by this loop — RULES.md rule 7).
**Verify:** `pnpm vitest run tests/atomic/insight/session-observe.test.ts`

### Task 3.6: `cortex sync` Rule 15 creates the registry once; `cortex init` scaffolds it

**Criterion:** `core-cli.sync` — "A missing id registry is created once from the files on disk"; "Knowledge content is byte-identical after sync"; `schema.id-registry` — "Sync creates the registry once and never rewrites it"; `core-cli.init` Rule 3 (registry skeleton — covered by the existing "Fresh init on an empty project succeeds end-to-end" case, extended)
**Files:** `tests/atomic/core-cli/sync.test.ts` (modify), `tests/spec/core-cli/sync.test.ts` (modify), `tests/atomic/core-cli/init.test.ts` (modify — assert `compass/registry.md` equals `REGISTRY_HEADER + '\n'` on a fresh init), `src/cli/sync.ts` (modify — a step between Rule 4 and Rule 5, `onProgress('id registry')`, summary field `registry: 'created (n rules, m bugs)' | 'present'`), `src/cli/init.ts` (modify — write the header-only registry in the skeleton step), `src/cli/templates.ts` (modify — `CORTEX_INDEXES['compass']` gains one bullet: `` - `registry.md` — every issued R-NNN and B-NNN, one line each; allocate with `cortex id next`. ``; keep the file under the §7.1 budget)
**Change:** As stated; sync never touches an existing registry. Note the `_index.md` template change means `cortex sync` in this repo will report `compass/_index.md` as localised (it is) — expected.
**Verify:** `pnpm vitest run tests/atomic/core-cli/sync.test.ts tests/spec/core-cli/sync.test.ts tests/atomic/core-cli/init.test.ts`

### Task 3.7: `schema.id-registry` spec-layer slice

**Criterion:** `schema.id-registry` — "Promote allocates through the registry" is batch 2's; every other criterion end to end through the CLI
**Files:** `tests/spec/schema/id-registry.spec.test.ts` (create)
**Change:** Drive `cortex init --no-llm --yes`, `cortex id next`, plant files, `cortex validate --json`, `cortex sync` in a temp project; assert the AC outcomes verbatim.
**Verify:** `pnpm vitest run tests/spec/schema/id-registry.spec.test.ts`

---

## Batch 2 — bug fields, the head-commit helper, promote, and archive-sourced promotions (~70 min; after batch 3)

### Task 2.1: `readHeadCommit`

**Criterion:** `compass.bug-currency` — "The helper reads a symbolic HEAD"; "The helper reads a packed ref, a detached head and a worktree"; "The helper never throws"
**Files:** `tests/atomic/compass/git-head.test.ts` (create), `src/compass/git-head.ts` (create)
**Change:** `export function readHeadCommit(root: string): string | null` per `compass.bug-currency` Rule 4 — `.git` dir or `gitdir:` file (one hop), `ref: <path>` → ref file, else `packed-refs` (in `commondir` when present, one hop), detached sha accepted, return `sha.slice(0, 7).toLowerCase()`, `null` on anything else. Wrap every read in try/catch.
**Verify:** `pnpm vitest run tests/atomic/compass/git-head.test.ts`

### Task 2.2: `check.bug` learns the three optional fields, shape only

**Criterion:** `compass.bug-currency` — "The three fields validate by shape only"; "A malformed stamp is an error; a bare status is not"
**Files:** `tests/atomic/schema/bug-currency-check.test.ts` (create — do not edit `validator.test.ts`, wave A owns it), `src/schema/checks/compass.ts` (modify — `checkBugs` only, three new blocks appended after the `affects` block; do not touch `checkRules` or the id/H1 lines wave A is editing)
**Change:** `owner`/`fix_in_flight` present and not a string → error (`clause: '§4.2'`, key named); `found_at_commit` present and not `/^[0-9a-f]{7,40}$/` → error. No warning for a `triaged` bug lacking either field.
**Verify:** `pnpm vitest run tests/atomic/schema/bug-currency-check.test.ts`

### Task 2.3: Promote allocates through the registry and stamps the commit

**Criterion:** `compass.bug-currency` — "Promote stamps the draft"; `schema.id-registry` — "Promote allocates through the registry"
**Files:** `tests/atomic/pulse/thread-cli.test.ts` (modify), `tests/spec/pulse/thread-cli.spec.test.ts` (modify), `src/pulse/thread-cli.ts` (modify)
**Change:** Delete `nextBugId`; the `--to compass/bugs` path calls `allocateId(root, 'bug', slug)` (batch 3) for the id **after** the slug is computed and **before** writing the file; add `found_at_commit: <sha>` to the drafted frontmatter when `readHeadCommit(root)` is non-null, no key otherwise. The drafted file must still pass `cortex validate` including `check.id-registry`.
**Verify:** `pnpm vitest run tests/atomic/pulse/thread-cli.test.ts tests/spec/pulse/thread-cli.spec.test.ts`

### Task 2.5: Promotions accept archive sources (B-020)

**Criterion:** `insight.promotion-mechanism` — "An archive-sourced promotion lands the gated write and the lineage, and touches no insight file"; "A promotion with a missing or unrecognised source refuses transactionally"; "An insight-sourced promotion lands the gated write, the lineage, and the promoted marker" (must stay green); `archive.ingest-skill` — "An emitted promotion is acceptable by the gate it targets (B-020)"
**Files:** `tests/spec/pulse/promotion-mechanism.spec.test.ts` (modify — red first: the archive-sourced case fails today with `Refusing promotion … must name the insight file`), `tests/atomic/pulse/promote.test.ts` (create if absent, else modify), `tests/spec/archive/ingest-skill.spec.test.ts` (modify or create — the end-to-end accept), `src/pulse/promote.ts` (modify), `src/pulse/review.ts` (modify — the promotion branch at ~637–651 only)
**Change:** In `promote.ts` rename `extractInsightSource` to `extractPromotionSource(sourceField): { kind: 'insight' | 'archive'; rel: string } | null` — the existing insight alternation (`insight/(anatomy|scopes/<s>/anatomy)/…\.md`, keep `map/` out) plus `/(?:^|\s)(?:\.cortex\/)?(archive\/documents\/[^\s/]+\/extracted\/[^\s]+\.md)/` → `{ kind: 'archive', rel: '.cortex/' + match }`; keep an `extractInsightSource` re-export for one release if anything else imports it (grep first). `planPromotion`: resolve the source per kind; refuse when the file does not exist (message: `Refusing promotion S-NNN: **Source:** must name the artefact being promoted — an insight entry (.cortex/insight/anatomy/**) or an archive extraction (.cortex/archive/documents/<id>/extracted/**)`; never mention `insight/map/`); for `archive` kind refuse when the target is not under `.cortex/compass/` or `.cortex/atlas/`; make `insightAbs`/`nextInsight` optional (`null` for archive kind); `injectSourceFrontmatter` receives the archive rel path unchanged. In `review.ts`: write the insight file only when `plan.insightAbs !== null`; the success line reads `promoted to <target>` without "marked the insight original" for archive kind. Rule-19 surfaces: the Source check, the refusal text, the trailer condition, the review.ts message; `check.pulse` unchanged.
**Verify:** `pnpm vitest run tests/spec/pulse/promotion-mechanism.spec.test.ts tests/atomic/pulse/promote.test.ts tests/atomic/pulse/review-cli.test.ts tests/spec/archive/ingest-skill.spec.test.ts`

### Task 2.4: `compass.bug-currency` spec-layer slice

**Criterion:** every `compass.bug-currency` criterion end to end
**Files:** `tests/spec/compass/bug-currency.spec.test.ts` (create)
**Change:** Temp project with a fake `.git`, a thread, `cortex thread promote`, `cortex validate --json`, `cortex why` (batch 1 — assert the `Bugs:` sub-line only if batch 1 has landed; otherwise assert the frontmatter).
**Verify:** `pnpm vitest run tests/spec/compass/bug-currency.spec.test.ts`

---

## Batch 4 — visibility, config keys, and the CLAUDE.md block (~70 min)

### Task 4.1: `check.config` learns `visibility` and `placement`

**Criterion:** `schema.visibility` — "The config shape is checked"
**Files:** `tests/atomic/schema/config-check.test.ts` (modify), `src/schema/checks/config.ts` (modify)
**Change:** `knownKeys` += `visibility`, `placement`. `visibility` present: must be an object; `repo` present and not in `['public','private','unknown']` → error (key `visibility.repo`); `allow` present and not an array of strings → error (key `visibility.allow`). `placement` present: object; `localNotesDir` present and not a string → error. Absent keys → nothing. Copy the `profile` block's shape.
**Verify:** `pnpm vitest run tests/atomic/schema/config-check.test.ts`

### Task 4.2: `check.visibility`

**Criterion:** `schema.visibility` — "Silent unless the repo is declared public"; "A public repo warns with the line, per family"; "Safe hosts and dev ports never match"; "Ignored files are out of scope, re-included ones are in"; "An allow glob silences a file and is reported"; "The per-file cap holds"
**Files:** `tests/atomic/schema/visibility-check.test.ts` (create), `src/schema/checks/visibility.ts` (create), `src/schema/validate.ts` (modify — register `checkVisibility(root)` and `checkIdRegistry(root)` after `checkBugs`; the report's `notes` (or an equivalent existing side channel — if `ValidationReport` has none, emit the allowed-file note as a `warning`-free `info`-style line in the CLI renderer only and document it in the test) carries `allowed by visibility.allow: <path>` per skipped file)
**Change:** Export `VISIBILITY_PATTERNS: { name: 'ipv4'|'host'|'port'|'ssh'|'account'; regex: RegExp }[]`, `VISIBILITY_CONTEXT_WORDS`, `VISIBILITY_SAFE_HOSTS`, `VISIBILITY_MAX_LINES_PER_FILE = 20`, with the regexes exactly as `schema.visibility` Rule 4 pins them (the `ipv4` exclusions and the `host` context/safe-host conditions applied in code around the regex). `checkVisibility(root)`: read config; return `[]` unless `visibility.repo === 'public'`; build an `ignore` instance from the root `.gitignore` and `.cortex/.gitignore` when present; walk `.cortex/compass/**` and `.cortex/atlas/**` for `.md`/`.yaml`, skip ignored paths and paths matching any `allow` glob (picomatch); scan line by line, one warning per matching line (`check: 'check.visibility'`, `clause: '§10.1'`, `location: { path, line }`, message per Rule 5), cap 20 per file then one `… and N more lines` warning. Deterministic order (sorted paths, ascending lines).
**Verify:** `pnpm vitest run tests/atomic/schema/visibility-check.test.ts`

### Task 4.3: Init writes `visibility` explicitly; the config default carries it

**Criterion:** `schema.visibility` — "Init writes the key explicitly; sync leaves an old config alone"
**Files:** `tests/atomic/core-cli/init.test.ts` (modify — the config-shape assertion at line ~224 gains `visibility: { repo: 'unknown', allow: [] }`), `tests/atomic/core-cli/sync.test.ts` (modify — "MINOR upgrade rewrites schemaVersion and nothing else" asserts no `visibility` key appears), `src/cli/templates.ts` (modify — `CONFIG_DEFAULTS.visibility = { repo: 'unknown', allow: [] }`; no `placement` default)
**Change:** As stated; `cortex sync` Rule 2 already rewrites `schemaVersion` only.
**Verify:** `pnpm vitest run tests/atomic/core-cli/init.test.ts tests/atomic/core-cli/sync.test.ts`

### Task 4.4: The CLAUDE.md block — roles, placement paragraph, mandate removed

**Criterion:** `core-cli.init` — "The block names the non-implementer roles, states placement once, and drops the mandate"; `core-cli.sync` Rule 3 (the "Missing CLAUDE.md block is repaired" case, extended to a config with `visibility.repo: "public"`)
**Files:** `tests/atomic/core-cli/claude-md-block.test.ts` (create), `tests/spec/core-cli/init.test.ts` (modify), `tests/spec/core-cli/sync.test.ts` (modify), `src/cli/templates.ts` (modify — `claudeMdBlock(projectName: string, config: Record<string, unknown> = {})`), `src/cli/scaffold.ts` (modify — `upsertClaudeMd(root, config?)` reads `cortex.config.json` itself when no config is passed, and passes it to `claudeMdBlock`), `src/cli/init.ts:561` and `src/cli/sync.ts:536` (modify — pass the parsed config, or rely on the read-inside default)
**Change:** In `claudeMdBlock`: (a) after "prompts that tell you what to read and when." insert the pinned sentence `This applies more, not less, to sessions that dispatch, review or plan rather than edit: everything reaches them as a claim, and \`compass/\` is where claims are checked.`; (b) after the protocol paragraph add the paragraph `**Placement:** durable knowledge lives in \`.cortex/\` (tracked, except \`atlas/sources/\`, \`pulse/\` and archived raw sources).` + `placementTail(config)` — ` \`<dir>\` is local and untracked: notes there do not travel — promote them into \`.cortex/\` instead of tracking the directory.` when `placement.localNotesDir` is a non-empty string, then ` This repository is public: compass and atlas carry pointers, never hosts, ports or account ids (RULES.md rule 20).` when `visibility.repo === 'public'`, or ` Repository visibility is unknown — set \`visibility.repo\` in \`cortex.config.json\`.` when it is `'unknown'` or absent, nothing when `'private'`; (c) delete the three-line "Before substantive work on any file … This is not optional." paragraph. Assert in the new atomic test that the rendered block for a default config is **no longer** (in characters) than the block the previous template rendered (hard-code the previous length as a constant in the test with a comment), that the three tails render as pinned, and that `check.claude-md` still passes on a fresh init.
**Verify:** `pnpm vitest run tests/atomic/core-cli/claude-md-block.test.ts tests/spec/core-cli/init.test.ts tests/spec/core-cli/sync.test.ts`

### Task 4.5: `schema.visibility` spec-layer slice

**Criterion:** every `schema.visibility` criterion end to end through `cortex validate --json`
**Files:** `tests/spec/schema/visibility.spec.test.ts` (create)
**Verify:** `pnpm vitest run tests/spec/schema/visibility.spec.test.ts`

### Task 4.6: Skill mirror and listing budget stay green

**Criterion:** `scaffolding.skill-listing-budget` (unchanged) — the four edited bundles remain byte-identical between `skills/` and `.claude/skills/` and within budget
**Files:** none (verification only; the skill edits are already made)
**Verify:** `pnpm vitest run tests/spec/scaffolding/skill-listing-budget.test.ts`

---

## Close-out (orchestrator, sequential)

1. `pnpm test` — full suite green (the only authoritative run).
2. `pnpm build`.
3. In this repo: `cortex sync` (rewrites the CLAUDE.md block from the new template; creates `.cortex/compass/registry.md` with `R-001`–`R-003` and `B-001`–`B-019`; expect `compass/_index.md` reported as localised), then `cortex scan` (rebuilds `recall-index.json` with the compass carriers), then `cortex validate` — expect zero errors; `check.visibility` silent (this repo's `visibility.repo` is unset → `unknown`; Pedro decides whether to set `private`, which is what `environment.md` says the remote is).
4. Hand-check `CLAUDE.md`: the role sentence present, one `**Placement:**` line, no "This is not optional".
5. Flip the three new specs and the amended ones' `status` per the build loop; update `.specflow/specs/_index.md` item 7 to "implemented".
6. Memory: one `project` note that `compass/registry.md` exists and `cortex id next` is the allocator (the specflow-bugs skill text says so; the memory is for sessions that skip the skill).
7. `B-020` → `status: resolved`, `resolved:` set, Resolution section filled (spec changes, tests, code) — `compass/preferences.md`: resolved bugs stay filed. Commit with the attribution trailer; report per RULES.md rule 19 with the surface list from the ground rules.
