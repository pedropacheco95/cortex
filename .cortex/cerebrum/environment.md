# Environment

Operational pointers only — aliases, profile names, tool locations. **Never secrets.**

(nothing recorded yet)

GitHub remote: github.com/pedropacheco95/cortex (private). Loops checking remote state (stale PRs, branches) use this.