# Archive register

One entry per ingested document — active and superseded. Updated by
`cortex-archive-ingest` on every ingestion or version update.

- `codebase-atlas-design` — active, kind `design-reference`, ingested 2026-07-10.
