# task-scoping

Project-scoped naming of Claude Desktop scheduled tasks (`<project-slug>-<short-hash>-<canonical-task-name>`) so one global `~/.claude/scheduled-tasks/` namespace can hold many projects' tasks without collision.

## Files

- src/cli/init.ts
- src/cli/task-scoping.ts
