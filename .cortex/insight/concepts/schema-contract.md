# schema-contract

The Violation/ValidationReport type shape every check returns and validate() aggregates, defined once in types.ts.

## Files

- src/schema/types.ts

## Related concepts

- validation-checks — co-members of cluster:schema-validator: every check returns the Violation shape types.ts defines
