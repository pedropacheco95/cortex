# carry-forward-by-normalised-name

A new candidate is matched against a prior pending report section by normaliseText(name) so its original S-NNN suggestion id is preserved across runs instead of being reissued.

## Files

- src/loops/skill-suggest.ts
- src/pulse/distil.ts
