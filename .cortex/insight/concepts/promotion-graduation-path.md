# promotion-graduation-path

distil.ts detects when a candidate pattern is already captured in insight prose (insightFileCovering) and redirects it to a promotion proposal instead of a fresh compass-rule proposal; promote.ts's planPromotion consumes that same promotion-provenance text format without either file importing the other.

## Files

- src/pulse/distil.ts
- src/pulse/promote.ts

## Related concepts

- pulse-proposal-gate — co-members of cluster:pulse-proposals: graduation routes through the same review/promote gate
