---
id: evidence.2026-09-16-usage
title: "Cortex usage over 54 sessions (2026-08-30 to 2026-09-16)"
date: 2026-09-16T13:54:51.365Z
kind: measurement
instrument: pulse.usage
window:
  from: 2026-08-30
  to: 2026-09-16
  sessions: 54
findings:
  - metric: searches.knowledge
    value: 59
  - metric: searches.machinery
    value: 51
  - metric: searches.document
    value: 147
  - metric: searches.other
    value: 519
  - metric: insight.file
    value: 6
    unit: invocations
  - metric: recall.recall
    value: 1
  - metric: recall.why
    value: 1
  - metric: reads.atlas-decisions
    value: 0
  - metric: reads.pulse-threads
    value: 0
  - metric: pointers.fired
    value: 2
  - metric: pointers.followed
    value: 0
  - metric: deferrals.deferred
    value: 1
  - metric: deferrals.proceeded
    value: 0
  - metric: deferrals.later
    value: 0
  - metric: deferrals.abandoned
    value: 1
  - metric: questions.before-consult
    value: 1
bears_on:
  - "schema:§5"
  - pulse.usage
supersedes:
  - 2026-09-15-usage.md
---

## Window

Computed over 54 sessions, 2026-08-30 to 2026-09-16.
Malformed transcript lines skipped: 0.

## `cortex insight` invocations

Counted from Bash command fields only — mentions in prose, specs, and design documents
are excluded.

- `cortex insight file`: 6

## `cortex recall` / `cortex why` invocations

Counted per command word from Bash command fields only, the same population as above.

- `cortex recall`: 1
- `cortex why`: 1

## `.cortex/` reads

Orientation reads: 69 across 54 sessions.
Loop machinery (`pulse/state`, `pulse/reports`): 98 — reported
separately because a loop reading its own worklist is not the assistant consulting
project knowledge.

Orientation reads by module:

- `compass/`: 27
- `insight/`: 30
- `pulse/`: 1

Tracked subdirectories (orientation reads, reported every run):

- `atlas/decisions/`: 0
- `pulse/threads/`: 0

Index reads — root `_index.md`: 11; module-level: 7.

## Searches targeting `.cortex/`

110 across 54 sessions.
This is the knowledge bucket plus the machinery bucket of the table below.

## Searches by target

A search is a command segment that is itself a `grep`/`rg`/`find` with a path operand, or a
Grep tool call with a path — pipe filters are not searches. Each segment counts once.

| Target | Searches |
|---|---|
| knowledge (`compass/`, `atlas/`, `insight/`, `archive/`) | 59 |
| machinery (`pulse/`, config, constellation, root `_index.md`) | 51 |
| document (`cortex-schema.md`, `.specflow/`, `RULES.md`, `CLAUDE.md`) | 147 |
| other | 519 |

## Pointer follow-through

Hook-injected `Recall:` / `Decided:` / `Evidence:` / `Open:` lines: fired 2, followed 0
across 54 sessions. Followed means a Read or search of the pointed path, the command
the line's `more:` tail named, or a thread verb on an `Open:` line's thread, within the
next 10 tool calls of the same session.

## Read deferrals

`Deferred:` lines (the read-deferral gate, `hooks.readDefer`): deferred 1, proceeded 0, later 0, abandoned 1
across 54 sessions. Proceeded means a Read of the deferred path within the next
3 tool calls of the same session; later means the first Read came after that window;
abandoned means no Read of it followed in the session. A zero row is the baseline while the flag is off.

proceed-rate: 0.00

## Questions asked before any consult

1 of 54 sessions.
Counted via `AskUserQuestion` tool calls only, so this figure is a **floor** — questions
asked in prose are not counted.
