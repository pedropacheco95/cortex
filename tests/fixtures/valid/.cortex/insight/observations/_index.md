# Observations — index

**Read this when:** you want the session-learned project context (audience,
scale, deployment, working style, stated intent) instead of re-deriving it
from scratch — grain-of-salt, ungated, written by cortex-loop-session-observe.

**What's here:**
- `scale.md` — expected load / user count, as stated in sessions.

**How to navigate:** each file is one themed, current-truth entry — read it
directly; don't treat it as a log.
